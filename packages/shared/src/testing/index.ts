// Sim test harness (SimHarness, NetSim, bots) lands in Phase 3. This subpath
// export exists now so apps/server and apps/client can depend on
// "@castle-clash/shared/testing" as a stable import path from the start.
export {};

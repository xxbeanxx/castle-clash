{
  "name": "@castle-clash/server",
  "version": "0.0.0",
  "private": true,
  "files": [
    "dist"
  ],
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsx watch src/index.ts",
    "format:check": "oxfmt --check",
    "format:fix": "oxfmt",
    "lint:check": "oxlint",
    "lint:fix": "oxlint --fix",
    "smoke-join": "tsx scripts/smoke-join.ts",
    "start": "node dist/index.js",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@castle-clash/shared": "workspace:*",
    "@colyseus/monitor": "0.18",
    "colyseus": "0.18",
    "express": "5.2"
  },
  "devDependencies": {
    "@colyseus/sdk": "0.18",
    "@colyseus/testing": "^0.18",
    "@types/express": "5.0",
    "@types/node": "^26.5",
    "@types/supertest": "^7.2",
    "supertest": "^7.2",
    "tsx": "^4.23",
    "typescript": "6.0",
    "vitest": "5.0"
  }
}